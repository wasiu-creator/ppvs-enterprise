{
  "name": "zkp-voting-system",
  "version": "1.0.0",
  "description": "Privacy-Preserving Voting System using Zero-Knowledge Proofs",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "node": "hardhat node",
    "deploy": "hardhat run scripts/deploy.js --network localhost",
    "demo": "node scripts/demo.js",
    "setup:circuit": "bash scripts/setup_circuit.sh"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "hardhat": "^2.22.0"
  },
  "dependencies": {
    "circomlibjs": "^0.1.7",
    "ffjavascript": "^0.3.0",
    "snarkjs": "^0.7.4"
  }
}
